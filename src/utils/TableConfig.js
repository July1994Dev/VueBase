const defaultColDef = {
  sortable: true,
  flex: 1,
  minWidth: 100,
  filter: true,
  resizable: true,
};

const gridOptions = {
  domLayout: "autoHeight",
};

export { defaultColDef, gridOptions };
